# Manual test workspace

Copy `TestWorkspace` to a disposable location before testing file operations.

Open the copied `TestWorkspace` folder as a workspace in Markive. Do not open
`00 Start Here.md` directly—standalone files have no workspace index, so
wikilinks and embeds cannot resolve.

Start with `01 Manual Test Checklist.md`.
